import { ZeroPlaceholderPipe } from './zero-placeholder.pipe';

describe('ZeroPlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new ZeroPlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
