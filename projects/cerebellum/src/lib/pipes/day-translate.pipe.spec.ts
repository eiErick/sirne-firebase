import { DayTranslatePipe } from './day-translate.pipe';

describe('DayTranslatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DayTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
